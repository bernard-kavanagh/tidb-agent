# See requirements-vercel.txt (Vercel) or requirements-ec2.txt (EC2/local)
# Install the one you need:
#   pip install -r requirements-ec2.txt    # local development
#   pip install -r requirements-vercel.txt # Vercel deployment
